{
  "name": "vacancy-live-e2e",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test tests/vacancy.spec.js --grep-invert \"map pin and card selection stay synchronized\" && playwright test tests/map-sync.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "1.55.1"
  }
}

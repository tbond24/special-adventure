const LISTING_DRAFT_VERSION=1;
const listingDraftTimers=new WeakMap();
function listingDraftKey(form){return `vacancy-listing-draft-v${LISTING_DRAFT_VERSION}:${currentUser?.id||'anonymous'}:${form.dataset.draftScope||'new-property'}`}
function listingDraftData(form){
  const skip=new Set(['address','publicLatitude','publicLongitude']);
  const read=controls=>Object.fromEntries([...controls].filter(control=>control.name&&control.type!=='file'&&!skip.has(control.name)).map(control=>[control.dataset.baseName||control.name,control.value]));
  const shared=read([...form.querySelectorAll('[name]')].filter(control=>!control.closest('.unit-editor')));
  const units=[...form.querySelectorAll('.unit-editor')].map(unit=>({...read(unit.querySelectorAll('[data-base-name]')),_requestId:unit.dataset.requestId}));
  return{version:LISTING_DRAFT_VERSION,scope:form.dataset.draftScope||'new-property',savedAt:new Date().toISOString(),shared,units};
}
function saveListingDraft(form,data=listingDraftData(form)){
  if(!currentUser||!form?.dataset.draftScope)return;
  const hasContent=Object.values(data.shared||{}).some(Boolean)||(data.units||[]).some(unit=>Object.values(unit).some(Boolean));
  if(hasContent)localStorage.setItem(listingDraftKey(form),JSON.stringify(data));
  const status=form.querySelector('.listing-draft-status small');if(status)status.textContent='Draft saved on this device';
}
function clearListingDraft(form,key=listingDraftKey(form)){localStorage.removeItem(key);const status=form.querySelector('.listing-draft-status small');if(status)status.textContent=''}
function renumberUnitEditors(form){[...form.querySelectorAll('.unit-editor')].forEach((unit,index)=>{unit.dataset.unitIndex=String(index);unit.querySelector('legend').textContent=`Unit ${index+1}`;unit.querySelectorAll('[data-base-name]').forEach(control=>control.name=index?`unit${index}_${control.dataset.baseName}`:control.dataset.baseName)})}
function restoreListingDraft(form){
  let draft;try{draft=JSON.parse(localStorage.getItem(listingDraftKey(form))||'null')}catch{return}
  if(!draft||draft.version!==LISTING_DRAFT_VERSION)return;
  while(form.querySelectorAll('.unit-editor').length<(draft.units?.length||1))form.querySelector('.add-unit')?.click();
  for(const [name,value] of Object.entries(draft.shared||{})){const control=form.querySelector(`[name="${CSS.escape(name)}"]`);if(control)control.value=value}
  [...form.querySelectorAll('.unit-editor')].forEach((unit,index)=>{unit.dataset.requestId=draft.units?.[index]?._requestId||unit.dataset.requestId;for(const [name,value] of Object.entries(draft.units?.[index]||{})){const control=unit.querySelector(`[data-base-name="${CSS.escape(name)}"]`);if(control)control.value=value}});
  const status=form.querySelector('.listing-draft-status small');if(status)status.textContent='Draft restored · exact address, map pin and photos are not stored';
}
function setupListingDraft(form,scope){
  form.dataset.draftScope=scope;
  if(!form.querySelector('.listing-draft-status')){
    const status=document.createElement('div');status.className='listing-draft-status notice wide';status.innerHTML='<span><strong>Unfinished listing</strong><small></small></span><button type="button" class="ghost">Discard draft</button>';
    form.querySelector('.listing-preview-action')?.before(status);
    status.querySelector('button').onclick=()=>{clearListingDraft(form);status.querySelector('small').textContent='Draft discarded'};
    const schedule=event=>{if(event.target.matches('input[type=file]'))return;clearTimeout(listingDraftTimers.get(form));listingDraftTimers.set(form,setTimeout(()=>saveListingDraft(form),250))};
    form.addEventListener('input',schedule);form.addEventListener('change',schedule);
  }
  restoreListingDraft(form);
}
const photoSelections=new WeakMap();
function selectedPhotoFiles(input){return photoSelections.get(input)||[...input.files]}
function renderPhotoSelection(input){
  let host=input.closest('label')?.querySelector('.photo-selection');if(!host){host=document.createElement('div');host.className='photo-selection';input.after(host)}
  host.querySelectorAll('img[src^="blob:"]').forEach(image=>URL.revokeObjectURL(image.src));const files=selectedPhotoFiles(input),shown=host.dataset.expanded==='true'?files:files.slice(0,4);host.classList.add('photo-selection-grid');host.innerHTML=shown.map((file,index)=>`<div class="photo-selection-item${index===0?' photo-main':''}"><img src="${URL.createObjectURL(file)}" alt="${escapeHtml(file.name)} preview"><span>${index===0?'<b>Main</b> · ':''}${escapeHtml(file.name)}</span>${index===3&&files.length>4&&host.dataset.expanded!=='true'?`<button type="button" class="photo-expand" data-photo-expand aria-label="Show all ${files.length} images">+${files.length-4}</button>`:''}<div>${index?`<button type="button" data-photo-main="${index}" aria-label="Use ${escapeHtml(file.name)} as main thumbnail">Main</button>`:''}<button type="button" data-photo-up="${index}" aria-label="Move ${escapeHtml(file.name)} earlier" ${index?'':'disabled'}>↑</button><button type="button" data-photo-down="${index}" aria-label="Move ${escapeHtml(file.name)} later" ${index===files.length-1?'disabled':''}>↓</button><button type="button" data-photo-remove="${index}" aria-label="Remove ${escapeHtml(file.name)}">×</button></div></div>`).join('');
  host.onclick=event=>{const button=event.target.closest('button');if(!button)return;event.preventDefault();event.stopPropagation();if(button.hasAttribute('data-photo-expand')){host.dataset.expanded='true';renderPhotoSelection(input);return}const next=[...selectedPhotoFiles(input)];if(button.dataset.photoRemove!==undefined)next.splice(Number(button.dataset.photoRemove),1);else if(button.dataset.photoMain!==undefined)next.unshift(next.splice(Number(button.dataset.photoMain),1)[0]);else{const from=Number(button.dataset.photoUp??button.dataset.photoDown),to=button.dataset.photoUp!==undefined?from-1:from+1;[next[from],next[to]]=[next[to],next[from]]}photoSelections.set(input,next);renderPhotoSelection(input)};
}
function setupPhotoInputs(form){form.querySelectorAll('input[type=file][name]').forEach(input=>{if(input.dataset.photoReady)return;input.dataset.photoReady='true';input.addEventListener('change',()=>{photoSelections.set(input,[...input.files]);renderPhotoSelection(input)})})}

async function optimiseListingImage(file){
  if(!file.type.startsWith('image/')||file.size<350000||typeof createImageBitmap!=='function')return file;
  try{
    const bitmap=await createImageBitmap(file),scale=Math.min(1,1920/Math.max(bitmap.width,bitmap.height));
    if(scale===1){bitmap.close();return file}
    const canvas=document.createElement('canvas');canvas.width=Math.round(bitmap.width*scale);canvas.height=Math.round(bitmap.height*scale);
    canvas.getContext('2d',{alpha:false}).drawImage(bitmap,0,0,canvas.width,canvas.height);bitmap.close();
    const type=file.type==='image/png'?'image/webp':file.type,blob=await new Promise(resolve=>canvas.toBlob(resolve,type,.82));
    if(!blob||blob.size>=file.size)return file;
    const ext=type==='image/webp'?'webp':type==='image/png'?'png':'jpg',stem=file.name.replace(/\.[^.]+$/,'');
    return new File([blob],`${stem}.${ext}`,{type,lastModified:file.lastModified});
  }catch{return file}
}
async function optimiseListingImages(files){return Promise.all([...files].map(optimiseListingImage))}

const listingServiceMeta={
  waterAvailable:['Water','water'],electricityAvailable:['Electricity','bolt'],securityAvailable:['Security','shield'],internetAvailable:['Internet / fibre','wifi'],
  smokingAllowed:['Smoking','smoking'],petsConsidered:['Pets','pets'],furnished:['Furnished','furnished'],ensuite:['Ensuite','shower'],billsIncluded:['Utilities included','bills'],
  smokingOverride:['Smoking','smoking'],petsOverride:['Pets','pets']
};
function listingControlIcon(name){return `<svg class="service-icon" aria-hidden="true"><use href="#icon-${name}"></use></svg>`}
function upgradeServiceChoice(select){
  if(!select||select.dataset.compactChoice)return;select.dataset.compactChoice='true';
  const name=select.dataset.baseName||select.name,meta=listingServiceMeta[name];if(!meta)return;
  const inherited=name.endsWith('Override'),label=select.closest('label'),row=document.createElement('div');
  row.className=`service-choice wide${inherited?' inherited-service':''}`;select.hidden=true;label.before(row);row.append(select);label.remove();
  row.insertAdjacentHTML('afterbegin',`<span class="service-choice-label">${listingControlIcon(meta[1])}<strong>${meta[0]}</strong><em ${inherited?'':'hidden'}>From property</em></span><button type="button" class="service-state" aria-label="Change ${meta[0]}"></button>`);
  const optional=['furnished','ensuite'].includes(name),state=row.querySelector('.service-state'),values=inherited||optional?['','true','false']:['true','false'];
  const render=()=>{state.dataset.value=select.value;state.textContent=select.value===''?(inherited?'From property':'Unknown'):select.value==='true'?'Yes':'No';state.setAttribute('aria-pressed',select.value==='true'?'true':'false');row.querySelector('em').hidden=!inherited||select.value!==''};
  state.onclick=()=>{select.value=values[(values.indexOf(select.value)+1)%values.length];select.dispatchEvent(new Event('change',{bubbles:true}));render()};render();
}
function upgradeUnitMoneyControls(scope){
  const field=name=>scope.querySelector(`[data-base-name="${name}"]`)||scope.querySelector(`[name="${name}"]`),label=name=>field(name)?.closest('label');
  const amount=field('rentAmount'),currency=field('rentCurrency'),period=field('rentPeriod');
  if(amount&&currency&&period){
    period.querySelectorAll('option').forEach(option=>option.textContent=({month:'Monthly',week:'Weekly',night:'Nightly'})[option.value]||option.textContent.replace(/^Per /,''));
    let row=scope.querySelector('.rent-control');if(!row){const first=label('rentAmount');row=document.createElement('div');row.className='wide inline-unit-control rent-control';row.innerHTML='<strong>Rent</strong><div class="inline-control-fields"></div>';first.before(row);[label('rentAmount'),label('rentCurrency'),label('rentPeriod')].forEach(old=>old?.remove())}
    row.querySelector('.inline-control-fields').append(currency,amount,period);amount.setAttribute('aria-label','Rent amount');currency.setAttribute('aria-label','Rent currency');period.setAttribute('aria-label','Rent frequency');
  }
  const deposit=field('deposit'),depositLabel=label('deposit');if(deposit&&depositLabel&&!depositLabel.classList.contains('deposit-control')){depositLabel.classList.add('wide','inline-unit-control','deposit-control');depositLabel.firstChild.textContent='Deposit / bond '}
  const stay=field('minimumStayWeeks'),stayLabel=label('minimumStayWeeks');if(stay&&stayLabel&&!stayLabel.classList.contains('stay-control')){const unit=document.createElement('select');unit.dataset.stayPeriod='true';unit.setAttribute('aria-label','Minimum stay unit');unit.innerHTML='<option value="week">Weeks</option><option value="month">Months</option><option value="year">Years</option>';stayLabel.classList.add('wide','inline-unit-control','stay-control');stayLabel.firstChild.textContent='Minimum stay ';stay.after(unit)}
}
function upgradeOccupantQuantity(scope){
  let input=scope.querySelector('[data-base-name="maxOccupants"], [name="maxOccupants"]');if(!input||input.dataset.quantityReady)return;if(input.tagName==='SELECT'){const replacement=document.createElement('input');replacement.type='number';replacement.name=input.name;replacement.value=input.value||'1';replacement.min='1';replacement.max='4';replacement.dataset.baseName=input.dataset.baseName||'maxOccupants';input.replaceWith(replacement);input=replacement}input.dataset.quantityReady='true';input.required=false;
  const label=input.closest('label'),advanced=document.createElement('details');advanced.className='advanced-unit-settings wide';advanced.innerHTML='<summary>Advanced settings <span>Optional</span></summary><div class="advanced-settings-body"></div>';label.before(advanced);advanced.querySelector('div').append(label);
  const control=document.createElement('div');control.className='quantity-control';input.before(control);control.append(input);input.setAttribute('aria-label','Maximum occupants');
  control.insertAdjacentHTML('afterbegin','<button type="button" data-step="-1" aria-label="Decrease maximum occupants">−</button>');control.insertAdjacentHTML('beforeend','<button type="button" data-step="1" aria-label="Increase maximum occupants">+</button>');
  control.querySelectorAll('button').forEach(button=>button.onclick=()=>{const maximum=Number(input.max||4),next=Math.max(1,Math.min(maximum,Number(input.value||1)+Number(button.dataset.step)));input.value=String(next);input.dispatchEvent(new Event('change',{bubbles:true}))});
}
function upgradeParkingQuantity(form){const input=form.querySelector('[name="parkingSpaces"]');if(!input||input.dataset.quantityReady)return;input.dataset.quantityReady='true';const label=input.closest('label'),control=document.createElement('div');control.className='quantity-control';input.before(control);control.append(input);input.setAttribute('aria-label','Parking spaces');control.insertAdjacentHTML('afterbegin','<button type="button" data-step="-1" aria-label="Decrease parking spaces">−</button>');control.insertAdjacentHTML('beforeend','<button type="button" data-step="1" aria-label="Increase parking spaces">+</button>');control.querySelectorAll('button').forEach(button=>button.onclick=()=>{input.value=String(Math.max(0,Math.min(50,Number(input.value||0)+Number(button.dataset.step))));input.dispatchEvent(new Event('change',{bubbles:true}))});label.classList.add('quantity-field')}
function upgradeListingControls(form){
  form.classList.add('listing-control-system');
  const scopes=[...form.querySelectorAll('.unit-editor')];if(!scopes.length)scopes.push(form);
  scopes.forEach(scope=>{upgradeUnitMoneyControls(scope);upgradeOccupantQuantity(scope)});
  upgradeParkingQuantity(form);
  form.querySelectorAll('select').forEach(upgradeServiceChoice);
}
function makeOptionalListingFields(form){
  const build=(scope,names,title)=>{const fields=names.map(name=>scope?.querySelector(`[data-base-name="${name}"], [name="${name}"]`)?.closest('label')).filter(Boolean);if(!fields.length)return;let advanced=scope.querySelector(':scope > .advanced-unit-settings');if(!advanced){advanced=document.createElement('details');advanced.className='advanced-unit-settings wide';advanced.innerHTML=`<summary>${title} <span>Optional</span></summary><div class="advanced-settings-body"></div>`;fields[0].before(advanced)}const body=advanced.querySelector('.advanced-settings-body');fields.forEach(label=>{if(label.closest('.optional-listing-field'))return;const name=label.querySelector('[name]')?.dataset.baseName||label.querySelector('[name]')?.name,row=document.createElement('div');row.className='optional-listing-field';row.innerHTML=`<button type="button" aria-expanded="false"><svg class="service-icon" aria-hidden="true"><use href="#icon-eye-off"></use></svg><span>${name==='household'?'Property notes':name==='description'?'About this unit':name==='availableFrom'?'Available from':'Minimum stay'}</span></button>`;label.before(row);row.append(label);label.hidden=true;row.querySelector('button').onclick=()=>{const open=label.hidden;label.hidden=!open;row.querySelector('button').setAttribute('aria-expanded',String(open));row.querySelector('use').setAttribute('href',open?'#icon-eye':'#icon-eye-off')}});};
  const unit=form.querySelector('.unit-editor')||form;build(form,['propertyNickname','household'],'Advanced property settings');build(unit,['availableFrom','minimumStayWeeks','description'],'Advanced unit settings');
}
function compactListingFields(form){form.querySelectorAll('label').forEach(label=>{if(label.classList.contains('wide')||label.querySelector('textarea,input[type=file]')||label.closest('.optional-listing-field'))return;const control=label.querySelector(':scope > input, :scope > select');if(control)label.classList.add('compact-field')});}
function setupAutomaticTitle(form){
  const input=form.querySelector('[data-base-name="roomName"], [name="roomName"]');if(!input||input.dataset.titleMode)return;input.dataset.titleMode='auto';
  const label=input.closest('label'),toggle=document.createElement('button');toggle.type='button';toggle.className='title-mode-toggle';
  const generated=()=>{const unit=form.querySelector('[data-base-name="unitType"], [name="unitType"]')?.value||'Unit',place=form.querySelector('[name="locality"]')?.value.trim()||'';return `${unit}${place?' in '+place:''}`};
  const sync=()=>{if(input.dataset.titleMode==='auto')input.value=generated()};
  const render=()=>{const automatic=input.dataset.titleMode==='auto';toggle.textContent=automatic?'Automatic':'Manual';toggle.setAttribute('aria-label',automatic?'Use manual listing title':'Use automatic listing title');toggle.dataset.automatic=String(automatic);input.readOnly=automatic;label.classList.add('title-mode-stable');sync()};
  toggle.onclick=()=>{input.dataset.titleMode=input.dataset.titleMode==='auto'?'manual':'auto';render();if(input.dataset.titleMode==='manual')input.focus()};label.prepend(toggle);
  form.querySelectorAll('[data-base-name="unitType"], [name="unitType"], [name="locality"]').forEach(control=>{control.addEventListener('input',sync);control.addEventListener('change',sync)});render();
}

function enhanceListingComposer(form){
  if(!form||form.dataset.composerReady)return;form.dataset.composerReady='true';
  const map=form.querySelector('#newPropertyMap');if(map){const heading=[...form.querySelectorAll('h2')].find(x=>x.textContent==='Location'),block=map.closest('.wide');if(heading&&block){heading.after(block);const fields=document.createElement('div');fields.className='wide form-grid property-identity';fields.innerHTML='<label>Property name<input name="propertyTitle" required placeholder="e.g. Sunrise Apartments"></label><label>Private manager nickname<input name="propertyNickname" maxlength="80" placeholder="e.g. Mum’s flats"><span class="muted">Only you see this.</span></label>';block.after(fields)}}
  const chooser=form.querySelector('#propertyChoice');if(chooser){const grid=document.createElement('div');grid.className='property-card-picker wide';const create=document.createElement('button');create.type='button';create.className='property-select-card property-create-card';create.innerHTML='<strong>＋ New property</strong>';create.onclick=()=>document.querySelector('#createProperty')?.click();grid.appendChild(create);[...chooser.options].forEach(option=>{const property=(window.__listingProperties||[]).find(item=>item.id===option.value),card=document.createElement('button');card.type='button';card.className='property-select-card';card.innerHTML=`<span><strong>${escapeHtml(property?.managerNickname||option.textContent.split(' — ')[0])}</strong><small>${escapeHtml(option.textContent.split(' — ')[1]||'')}</small></span><b>＋ Unit</b>`;card.onclick=()=>{chooser.value=option.value;chooser.dispatchEvent(new Event('change'));form.hidden=false;grid.querySelectorAll('button').forEach(x=>x.classList.toggle('active',x===card))};grid.appendChild(card)});form.before(grid);form.hidden=true}const publish=form.querySelector('button.primary.wide');if(!publish)return;const button=document.createElement('button');button.type='button';button.className='ghost wide listing-preview-action';button.textContent='Preview listing';const output=document.createElement('section');output.className='listing-draft-preview wide';output.hidden=true;button.onclick=()=>{const data=new FormData(form),unit=form.querySelector('.unit-editor'),read=name=>unit?.querySelector(`[data-base-name="${name}"]`)?.value||data.get(name)||'';output.innerHTML=`<div class="muted">Preview</div><h3>${escapeHtml(read('roomName')||'Untitled unit')}</h3><strong class="listing-price">${escapeHtml(read('rentCurrency'))} ${Number(read('rentAmount')||0).toLocaleString()} / ${escapeHtml(read('rentPeriod')||'month')}</strong><p>${escapeHtml(read('unitType')||'Unit')} · ${escapeHtml(String(data.get('locality')||''))}, ${escapeHtml(String(data.get('city')||''))}</p><p class="muted">${escapeHtml(read('description')||'Add a description to complete this preview.')}</p>`;output.hidden=false;output.scrollIntoView({behavior:'smooth',block:'nearest'})};publish.before(button,output);
  const headings=[...form.querySelectorAll('h2')],location=headings.find(x=>x.textContent==='Location'),property=headings.find(x=>x.textContent==='Property'),units=headings.find(x=>x.textContent.includes('Unit'));
  [[location,'listing-location'],[property,'listing-property'],[units,'listing-units'],[output,'listing-preview']].forEach(([element,id])=>{if(element)element.id=id});
  const steps=document.createElement('nav');steps.className='listing-steps wide';steps.setAttribute('aria-label','Listing steps');steps.innerHTML=`${property?'<button type="button" data-target="listing-property"><b>1</b> Property</button>':''}<button type="button" data-target="listing-units"><b>${property?2:1}</b> Listing</button>${location?`<button type="button" data-target="listing-location"><b>${property?3:2}</b> Location</button>`:''}<button type="button" data-target="listing-preview"><b>${property?4:3}</b> Preview</button>`;
  steps.onclick=event=>{const control=event.target.closest('[data-target]');if(!control)return;if(control.dataset.target==='listing-preview')button.click();else document.getElementById(control.dataset.target)?.scrollIntoView({behavior:'smooth',block:'start'})};
  form.prepend(steps);setupPhotoInputs(form);upgradeListingFlow(form,steps,button,output);upgradeListingControls(form);makeOptionalListingFields(form);setupAutomaticTitle(form);compactListingFields(form);
  const photoInput=form.querySelector('input[type="file"][name="images"],input[type="file"][data-base-name="images"]');
  if(photoInput){const quick=document.createElement('button');quick.type='button';quick.className='quick-photo-start';quick.innerHTML=`${listingControlIcon('camera')}<span>Add photos</span>`;quick.setAttribute('aria-label','Add photos');quick.onclick=()=>{const section=photoInput.closest('.composer-section');form.querySelectorAll('.composer-section').forEach(item=>item.open=item===section);photoInput.click()};steps.after(quick)}
  const actions=document.createElement('div');actions.className='listing-submit-actions wide';const save=document.createElement('button');save.type='button';save.className='save-draft-action';save.textContent='Save draft';save.onclick=()=>{saveListingDraft(form);toast('Draft saved on this device')};publish.before(actions);actions.append(save,publish);
}

function upgradeListingFlow(form,steps,previewButton,previewOutput){
  form.classList.add('guided-listing');
  const available=form.querySelector('[name="availableFrom"]');if(available&&!available.value){available.value=new Date().toISOString().slice(0,10);available.min=available.value}
  const roomName=form.querySelector('[name="roomName"]');if(roomName){roomName.required=false;roomName.closest('label').firstChild.textContent='Listing title (optional)';roomName.placeholder='Generated from unit type and location'}
  const description=form.querySelector('[name="description"]');if(description){description.required=false;description.placeholder='What makes this unit a good fit?'}
  const household=form.querySelector('[name="household"]');if(household){household.required=false;household.closest('label').firstChild.textContent='What should renters know about this property?';const hint=document.createElement('span');hint.className='muted';hint.textContent='Shared spaces, access, surroundings, who lives here or house expectations.';household.after(hint)}
  const minStay=form.querySelector('[name="minimumStayWeeks"]');if(minStay){minStay.value='';minStay.placeholder='No minimum';minStay.closest('label').classList.add('minimum-stay-field')}
  form.querySelectorAll('[name="rentAmount"],[name="deposit"]').forEach(input=>{input.type='text';input.inputMode='numeric';input.addEventListener('blur',()=>{const value=Number(input.value.replace(/,/g,''));input.value=Number.isFinite(value)&&value?value.toLocaleString('en-US'):''});input.addEventListener('focus',()=>input.value=input.value.replace(/,/g,''))});
  const unit=form.querySelector('.unit-editor');
  if(unit){
    const field=name=>unit.querySelector(`[data-base-name="${name}"]`)||unit.querySelector(`[name="${name}"]`),label=name=>field(name)?.closest('label');
    const amount=field('rentAmount'),currency=field('rentCurrency'),period=field('rentPeriod'),amountLabel=label('rentAmount'),currencyLabel=label('rentCurrency'),periodLabel=label('rentPeriod');
    if(amount&&currency&&period&&amountLabel){const row=document.createElement('div');row.className='wide inline-unit-control rent-control';row.innerHTML='<strong>Rent</strong><div class="inline-control-fields"></div>';const fields=row.lastElementChild;amount.setAttribute('aria-label','Rent amount');amountLabel.before(row);fields.append(period,currency,amount);[periodLabel,currencyLabel,amountLabel].forEach(old=>old?.remove())}
    const deposit=field('deposit'),depositLabel=label('deposit');if(deposit&&depositLabel){depositLabel.classList.add('wide','inline-unit-control','deposit-control');depositLabel.firstChild.textContent='Deposit / bond ';}
    const stay=field('minimumStayWeeks'),stayLabel=label('minimumStayWeeks');if(stay&&stayLabel){const periodSelect=document.createElement('select');periodSelect.dataset.stayPeriod='true';periodSelect.setAttribute('aria-label','Minimum stay period');periodSelect.innerHTML='<option value="week">Weeks</option><option value="month">Months</option><option value="year">Years</option>';stayLabel.classList.add('wide','inline-unit-control','stay-control');stayLabel.firstChild.textContent='Minimum stay ';stay.after(periodSelect)}
  }

  const booleanNames=['waterAvailable','electricityAvailable','securityAvailable','internetAvailable','smokingAllowed','petsConsidered','furnished','ensuite','billsIncluded'];
  for(const name of booleanNames){const select=form.querySelector(`[name="${name}"]`);if(!select)continue;const label=select.closest('label'),labelText=label.firstChild.textContent.trim(),toggle=document.createElement('button');toggle.type='button';toggle.className='choice-toggle';const render=()=>{const on=select.value==='true';toggle.dataset.on=String(on);toggle.setAttribute('aria-pressed',String(on));toggle.innerHTML=`<span>${labelText}</span><b>${on?'Yes':'No'}</b>`};toggle.onclick=()=>{select.value=select.value==='true'?'false':'true';select.dispatchEvent(new Event('change',{bubbles:true}));render()};select.hidden=true;label.firstChild.textContent='';label.classList.add('toggle-label');label.appendChild(toggle);render()}

  let manualMode=false;const map=form.querySelector('#newPropertyMap');if(map){map.parentElement.querySelector('strong')?.remove();const locationBody=map.parentElement,manual=document.createElement('div');manual.className='manual-location-choice';manual.innerHTML=`<button type="button" class="location-pick-action" aria-label="Use current location">${listingControlIcon('location-arrow')}</button><span>OR</span><button type="button" class="ghost">Enter address manually</button>`;locationBody.after(manual);manual.querySelector('.location-pick-action').onclick=()=>{if(!navigator.geolocation){toast('Location is not supported');return}const control=manual.querySelector('.location-pick-action');control.disabled=true;navigator.geolocation.getCurrentPosition(position=>{map._vacancySetLocation?.(position.coords.latitude,position.coords.longitude,true);control.disabled=false;toast('Current location selected')},()=>{control.disabled=false;toast('Location permission was not granted')},{enableHighAccuracy:false,timeout:8000,maximumAge:300000})};const locationNames=['region','city','locality','landmark','postal','address'];const fields=locationNames.map(name=>form.querySelector(`[name="${name}"]`)?.closest('label')).filter(Boolean);fields.forEach(field=>field.classList.add('manual-location-field','manual-location-hidden'));manual.querySelector('.ghost').onclick=()=>{const hidden=fields.some(field=>field.classList.contains('manual-location-hidden'));manualMode=hidden;fields.forEach(field=>field.classList.toggle('manual-location-hidden',!hidden));manual.querySelector('.ghost').textContent=hidden?'Use map selection':'Enter address manually'}}

  const headings=[...form.querySelectorAll('h2')],location=headings.find(x=>x.textContent==='Location'),property=headings.find(x=>x.textContent==='Property'),units=headings.find(x=>x.textContent.includes('Unit'));
  const wrap=(heading,boundary,title)=>{if(!heading)return null;const details=document.createElement('details'),summary=document.createElement('summary'),body=document.createElement('div');details.className='composer-section wide';summary.innerHTML=`<span class="section-state" aria-hidden="true"></span><strong>${title}</strong><small>Tap to open</small>`;body.className='composer-section-body form-grid';heading.before(details);details.append(summary,body);details.addEventListener('toggle',()=>summary.querySelector('small').textContent=details.open?'Tap to close':'Tap to open');let node=heading.nextElementSibling;heading.remove();while(node&&node!==boundary){const next=node.nextElementSibling;body.append(node);node=next}return details};
  const unitSection=wrap(units,previewButton,'Unit & availability'),propertySection=wrap(property,unitSection,'Property details'),locationSection=wrap(location,propertySection,'Location');
  const identity=form.querySelector('.property-identity');if(identity&&propertySection)propertySection.querySelector('.composer-section-body').prepend(...identity.children),identity.remove();
  if(locationSection&&propertySection)locationSection.before(propertySection,unitSection);
  const sections=[propertySection,unitSection,locationSection].filter(Boolean);const photoField=unitSection?.querySelector('input[type="file"]')?.closest('label');if(photoField)unitSection.querySelector('.composer-section-body').prepend(photoField);sections.forEach((section,index)=>{const next=document.createElement('button');next.type='button';next.className='primary section-continue';next.textContent=index===sections.length-1?'Review listing':'Continue';section.querySelector('.composer-section-body').append(next);next.onclick=async()=>{const required=[...section.querySelectorAll('[required]')],invalid=required.find(control=>!control.checkValidity());if(invalid){invalid.reportValidity();return}if(section===locationSection&&map&&(!form.elements.publicLatitude?.value||!form.elements.publicLongitude?.value)){if(!manualMode){toast('Choose a location on the map');return}const query=[form.elements.address?.value,form.elements.locality?.value,form.elements.city?.value,form.elements.region?.value].filter(Boolean).join(', ');next.disabled=true;next.textContent='Finding location…';try{const response=await fetch(`/api/geocode?q=${encodeURIComponent(query)}`),place=await response.json();if(!response.ok)throw new Error(place.error||'Location not found');map._vacancySetLocation?.(place.lat,place.lon,false)}catch(error){toast(error.message);return}finally{next.disabled=false;next.textContent='Continue'}}if(roomName&&!roomName.value.trim()){const type=form.querySelector('[name="unitType"]')?.value||'Unit',place=form.querySelector('[name="locality"]')?.value||'';roomName.value=`${type}${place?' in '+place:''}`}
      section.dataset.complete='true';section.open=false;const following=sections[index+1];if(following){following.open=true;following.scrollIntoView({behavior:'smooth',block:'start'})}else previewButton.click();updateGuidedSteps()}});if(sections[0])sections[0].open=true;
  form.addEventListener('vacancy:location-used',event=>{if(!locationSection||!propertySection)return;let chosen=propertySection.querySelector('.chosen-property-address');if(!chosen){chosen=document.createElement('p');chosen.className='chosen-property-address';propertySection.querySelector('.composer-section-body').prepend(chosen)}chosen.textContent=event.detail?.address||[form.elements.locality?.value,form.elements.city?.value].filter(Boolean).join(', ');locationSection.dataset.complete='true';locationSection.open=false;updateGuidedSteps()});
  function updateGuidedSteps(){[...steps.querySelectorAll('button')].forEach((step,index)=>{const complete=sections[index]?.dataset.complete==='true';step.classList.toggle('complete',complete);if(complete)step.querySelector('b').textContent='✓'})}
  steps.onclick=event=>{const control=event.target.closest('button');if(!control)return;const index=[...steps.children].indexOf(control);if(sections[index]){sections.forEach((section,i)=>section.open=i===index);sections[index].scrollIntoView({behavior:'smooth',block:'start'})}else previewButton.click()};
  const publish=form.querySelector('button.primary.wide');if(publish){publish.dataset.idleLabel=publish.textContent;form.addEventListener('submit',()=>{publish.textContent='Publishing…';publish.setAttribute('aria-busy','true')});new MutationObserver(()=>{if(!publish.disabled){publish.textContent=publish.dataset.idleLabel;publish.removeAttribute('aria-busy')}}).observe(publish,{attributes:true,attributeFilter:['disabled']})}
}

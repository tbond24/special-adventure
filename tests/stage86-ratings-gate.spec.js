const {test,expect}=require('@playwright/test');const APP=process.env.VACANCY_E2E_URL||'http://127.0.0.1:4173';
test('profile states ratings are unavailable instead of inventing a score',async({page})=>{await page.route('**/rest/v1/vacancies?**',route=>route.fulfill({status:200,contentType:'application/json',body:'[]'}));await page.goto(`${APP}/#home`);await page.waitForFunction(()=>booting===false);await page.evaluate(async()=>{currentUser={id:'member',email:'member@example.com'};VACANCY_BACKEND.profile=async()=>({display_name:'Member',email_confirmed:true});VACANCY_BACKEND.blockedUsers=async()=>[];VACANCY_BACKEND.adminMembership=async()=>false;await renderAccount()});await expect(page.locator('.account-identity small')).toHaveText('Ratings are not available yet');await expect(page.locator('body')).not.toContainText(/★|5\.0|rating score/i)});

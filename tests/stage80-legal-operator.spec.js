const {test,expect}=require('@playwright/test');
const APP=process.env.VACANCY_E2E_URL||'http://127.0.0.1:4173';
test('legal identity is never fabricated and renders only when complete',async({page})=>{await page.route('**/rest/v1/vacancies?**',route=>route.fulfill({status:200,contentType:'application/json',body:'[]'}));await page.goto(`${APP}/#privacy`);await page.waitForFunction(()=>booting===false);await expect(page.locator('.legal-operator')).toHaveCount(0);await page.evaluate(()=>{window.VACANCY_LEGAL_OPERATOR={legalName:'Example Operator',serviceAddress:'1 Example Road',jurisdiction:'Example State',privacyEmail:'privacy@example.test',legalEmail:'legal@example.test'};renderInformationPage('privacy')});await expect(page.locator('.legal-operator')).toContainText('Example Operator');await expect(page.locator('.legal-operator')).toContainText('Example State');await expect(page.locator('.legal-operator a')).toHaveCount(2)});

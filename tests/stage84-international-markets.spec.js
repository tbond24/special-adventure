const {test,expect}=require('@playwright/test');const APP=process.env.VACANCY_E2E_URL||'http://127.0.0.1:4173';
test('country-aware defaults expand beyond the six launch markets without moving the map',async({page})=>{await page.route('**/rest/v1/vacancies?**',route=>route.fulfill({status:200,contentType:'application/json',body:'[]'}));await page.goto(`${APP}/#home`);await page.waitForFunction(()=>booting===false);const data=await page.evaluate(()=>({count:Object.keys(MARKETS).length,canada:marketForCountry('Canada'),india:marketForCountry('IN'),uae:marketForCountry('UAE'),currencies:[...new Set(Object.values(MARKETS).map(item=>item.currency))],center:market().center}));expect(data.count).toBeGreaterThanOrEqual(18);expect(data.canada.currency).toBe('CAD');expect(data.india.currency).toBe('INR');expect(data.uae.currency).toBe('AED');expect(data.currencies).toContain('EUR');await expect(page.locator('#marketSelect option')).toHaveCount(data.currencies.length);expect(await page.evaluate(()=>market().center)).toEqual(data.center)});

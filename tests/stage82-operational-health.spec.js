const {test,expect}=require('@playwright/test');const APP=process.env.VACANCY_E2E_URL||'http://127.0.0.1:4173';
test('AAL2 admin sees delivery storage and error health without private data',async({page})=>{await page.route('**/rest/v1/vacancies?**',route=>route.fulfill({status:200,contentType:'application/json',body:'[]'}));await page.goto(`${APP}/#home`);await page.waitForFunction(()=>booting===false);await page.evaluate(async()=>{currentUser={id:'admin',email:'admin@example.com'};VACANCY_BACKEND.adminMembership=async()=>true;VACANCY_BACKEND.assuranceLevel=()=> 'aal2';VACANCY_BACKEND.adminDashboard=async()=>({open_reports:0,oldest_report_hours:0,active_listings:2,users_total:3,properties:2,units:2,enquiries_7d:1,listings_7d:2,errors_24h:0});VACANCY_BACKEND.adminSearch=async()=>({users:[],listings:[]});VACANCY_BACKEND.adminReports=async()=>[];VACANCY_BACKEND.adminAuditLog=async()=>[];VACANCY_BACKEND.adminDailyMetrics=async()=>({total:0,days:7,series:[]});VACANCY_BACKEND.adminOperationalHealth=async()=>({email_sent:100,email_delivered:98,email_bounced:2,email_complained:0,email_failed:0,storage_bytes:10485760,client_errors:1});await renderAdmin()});const panel=page.locator('#operationalHealth');await expect(panel).toContainText('98');await expect(panel).toContainText('2.0%');await expect(panel).toContainText('10.0 MB');await expect(panel).not.toContainText('admin@example.com')});

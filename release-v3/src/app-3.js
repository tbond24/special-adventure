function renderDetail(id){VACANCY_BACKEND.trackEvent('vacancy_opened',id,`#detail/${id}`);const v=vacancies.find(item=>item.id===id);if(!v){layout('<div class="empty">Vacancy not found.</div>');return}const location=[v.property.suburb,v.property.city,v.property.state].filter(Boolean).join(', ');const infra=[v.property.waterAvailable?'Water available':null,v.property.electricityAvailable?'Electricity':null,v.property.securityAvailable?'Security':null,v.property.internetAvailable?'Internet':null].filter(Boolean);layout(`<section class="detail-hero"><div class="detail-photo" style="${v.room.media?.[0]?.url?`background-image:url(${v.room.media[0].url});background-size:cover;background-position:center`:``}"></div><div class="panel"><div class="muted">${escapeHtml(location)}</div><h1>${escapeHtml(v.room.name)}</h1><div class="price">${formatListingPrice(v)}</div>${v.property.landmark?`<p class="muted">Near ${escapeHtml(v.property.landmark)}</p>`:''}<p>${escapeHtml(v.room.description||'')}</p><div class="facts"><span class="fact">${escapeHtml(v.room.roomType||'Unit')}</span><span class="fact">${v.room.furnished?'Furnished':'Unfurnished'}</span>${v.room.ensuite?'<span class="fact">Ensuite</span>':''}${infra.map(x=>`<span class="fact">${x}</span>`).join('')}</div><p class="muted">Available ${v.availableFrom}${v.minimumStayWeeks?` · Minimum ${v.minimumStayWeeks} weeks`:''}</p><div class="row"><button class="ghost" id="saveDetail">${saved.has(v.id)?'Saved ✓':'Save'}</button><button class="primary" id="enquire">Enquire</button></div><div class="row"><button class="ghost" id="reportListing">Report listing</button><button class="ghost" id="blockLister">Block lister</button></div></div></section><section class="split"><div class="panel"><h2>About the property</h2><p>${escapeHtml(v.property.householdSummary||'Property details coming soon.')}</p><strong>${escapeHtml(v.owner.displayName)}</strong><p class="muted">${escapeHtml(v.owner.bio)}</p></div><div class="panel"><h2>Property facts</h2><div class="property-facts"><details open><summary>Essentials</summary><div class="fact-body"><p>${escapeHtml(v.property.propertyType||'Property')} · ${escapeHtml(v.room.roomType||'Unit')}</p><p class="muted">Up to ${v.room.maxOccupants||1} occupant${(v.room.maxOccupants||1)>1?'s':''}${v.property.parkingSpaces>0?` · ${v.property.parkingSpaces} parking space${v.property.parkingSpaces>1?'s':''}`:''}</p></div></details><details><summary>Utilities & access</summary><div class="fact-body"><div class="facts">${infra.map(x=>`<span class="fact">${x}</span>`).join('')||'<span class="muted">No utility details yet.</span>'}</div></div></details><details><summary>Rules</summary><div class="fact-body"><p class="muted">${v.property.petsConsidered?'Pets considered':'Pets not confirmed'} · ${v.property.smokingAllowed?'Smoking allowed':'No smoking'}</p></div></details></div>${v.deposit!=null?`<p class="muted">Deposit ${marketForCountry(v.property.country).currencyLabel} ${Number(v.deposit).toLocaleString(marketForCountry(v.property.country).locale)}</p>`:''}</div></section>${(()=>{const siblings=vacancies.filter(x=>x.property.id===v.property.id&&x.id!==v.id);return siblings.length?`<section class="panel"><div class="section-head"><div><h2>Other available units here</h2><p class="muted">Sibling units under the same property</p></div></div><div class="unit-siblings">${siblings.map(x=>`<button class="sibling-card" data-sibling="${x.id}"><strong>${escapeHtml(x.room.name)}</strong><div class="muted">${escapeHtml(x.room.roomType||'Unit')}</div><div class="price">${formatListingPrice(x)}</div></button>`).join('')}</div></section>`:''})()}`);document.querySelector('#saveDetail').onclick=()=>toggleSave(v.id);document.querySelector('#enquire').onclick=()=>currentUser?nav('enquire',v.id):nav('auth');document.querySelector('#reportListing').onclick=async()=>{if(!currentUser){nav('auth');return}const reason=prompt('Reason for report (for example: scam, inaccurate, harassment)');if(!reason)return;try{await VACANCY_BACKEND.reportVacancy(v.id,v.owner.id,reason);toast('Report submitted')}catch(err){toast(err.message)}};document.querySelector('#blockLister').onclick=async()=>{if(!currentUser){nav('auth');return}if(v.owner.id===currentUser.id){toast('This is your listing');return}if(!confirm('Block this lister?'))return;try{await VACANCY_BACKEND.blockUser(v.owner.id);toast('User blocked')}catch(err){toast(err.message)}};document.querySelectorAll('[data-sibling]').forEach(b=>b.onclick=()=>nav('detail',b.dataset.sibling));}
function renderEnquire(id){if(!currentUser){nav('auth');return}const v=vacancies.find(x=>x.id===id); if(!v){layout('<div class="empty">Vacancy not found.</div>');return}if(v.owner.id===currentUser.id){layout('<div class="empty">You cannot enquire on your own vacancy.</div>');return}layout(`<section class="split"><div class="panel"><div class="muted">${escapeHtml(v.property.suburb)}</div><h1>${escapeHtml(v.room.name)}</h1><div class="price">${formatListingPrice(v)}</div><p>${escapeHtml(v.property.householdSummary||'')}</p></div><form id="enquiryForm" class="panel form-grid"><h2 class="wide">Contact the household</h2><label>Move-in date<input name="moveIn" type="date" required></label><label>Planned stay (weeks)<input name="stayWeeks" type="number" min="1" value="26"></label><label class="wide">A little about you<textarea name="intro" maxlength="600" placeholder="Work, study, routine, what you are looking for"></textarea></label><label class="wide">Message<textarea name="message" required maxlength="1200" placeholder="Hi, I would love to organise a viewing..."></textarea></label><button class="primary wide">Send enquiry</button></form></section>`);document.querySelector('#enquiryForm').onsubmit=async e=>{e.preventDefault();try{const f=Object.fromEntries(new FormData(e.target));await VACANCY_BACKEND.startEnquiry(id,f);VACANCY_BACKEND.trackEvent('enquiry_sent',id,location.hash);toast('Enquiry sent');nav('messages')}catch(err){toast(err.message)}}}
async function renderMessages(){if(!currentUser){nav('auth');return}layout('<div class="section-head"><h1>Messages</h1></div><div id="conversationHost" class="empty">Loading conversations…</div>');const host=document.querySelector('#conversationHost');try{const rows=await VACANCY_BACKEND.conversations();if(!rows.length){host.outerHTML='<div class="empty">No conversations yet.</div>';return}const selected=route.id?rows.find(r=>r.id===route.id)||rows[0]:rows[0];const threadItems=rows.map(r=>`<div class="thread-item ${r.id===selected.id?'active':''}" data-thread="${r.id}"><strong>${escapeHtml(r.vacancies.rooms.name)}</strong><div class="muted">${escapeHtml(r.vacancies.rooms.properties.suburb)}</div></div>`).join('');const ownerId=selected.vacancies.rooms.properties.owner_id; const firstSender=selected.messages[0]?.sender_id; const otherId=currentUser.id===ownerId?firstSender:ownerId;const bubbles=selected.messages.map(m=>`<div class="bubble ${m.sender_id===currentUser.id?'me':''}">${escapeHtml(m.body)}</div>`).join('');host.outerHTML=`<section class="conversation"><aside class="thread-list">${threadItems}</aside><div class="chat"><div class="muted">Move-in ${selected.requested_move_in||'not specified'}${selected.stay_weeks?` · ${selected.stay_weeks} weeks`:''}</div>${selected.renter_intro?`<div class="notice">${escapeHtml(selected.renter_intro)}</div>`:''}<div class="chat-log">${bubbles}</div><form id="chatForm" class="chat-form"><input name="body" required maxlength="1200" placeholder="Write a message"><button class="primary">Send</button></form>${otherId?`<div class="row"><button class="danger" id="blockConversation">Block user</button></div>`:''}</div></section>`;document.querySelectorAll('[data-thread]').forEach(x=>x.onclick=()=>nav('messages',x.dataset.thread));document.querySelector('#chatForm').onsubmit=async e=>{e.preventDefault();const body=new FormData(e.target).get('body');try{await VACANCY_BACKEND.sendMessage(selected.id,body);await renderMessages()}catch(err){toast(err.message)}};const bb=document.querySelector('#blockConversation');if(bb)bb.onclick=async()=>{if(!confirm('Block this user? You will no longer be able to message each other.'))return;try{await VACANCY_BACKEND.blockUser(otherId);toast('User blocked');await renderMessages()}catch(err){toast(err.message)}}}catch(err){host.textContent=err.message}}
function renderSaved(){if(!currentUser){nav('auth');return}const rows=vacancies.filter(v=>saved.has(v.id));layout(`<div class="section-head"><h1>Saved rooms</h1><span class="muted">${rows.length}</span></div><section class="grid">${rows.length?rows.map(card).join(''):'<div class="empty wide">Nothing saved yet.</div>'}</section>`);bindCards()}

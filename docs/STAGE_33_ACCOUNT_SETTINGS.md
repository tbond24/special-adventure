# Stage 33 — You settings hierarchy

Reddit-style line items ranked above collapsible cards and a settings modal because the current preference set is short. Preference and account actions now use separators instead of nested bright containers, with theme-aware transparent surfaces. The existing actions and backend behavior are unchanged.

Dark/light responsive route audit: **2/2 passed**. Production unchanged.
